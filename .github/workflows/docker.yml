name: Docker

on:
  schedule:
    - cron: "0 0 * * *"
  push:
    branches:
      - master
    paths:
      - ".github/workflows/docker.yml"
      - "docker/**"
  pull_request:
    branches:
      - master
    paths:
      - ".github/workflows/docker.yml"
      - "docker/**"

jobs:
  docker-debian11-amd64:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
    - uses: actions/checkout@v3
    - name: Config
      id: docker-build-config
      env:
        JOB:  ${{ github.job }}
        REPO: ${{ github.repository }}
      run: |
        set -e
        echo "TAG=$REPO-build-$(cut -d- -f2 <<< "$JOB")" >> "$GITHUB_OUTPUT"
        cut -d- -f2 <<< "$JOB"                      \
        | sed 's/\([0-9]\)/:\1/'                    \
        | sed 's/\([0-9][0-9]\)\([0-9]\)/\1\.\2/'   \
        | sed 's/^/BASE_IMAGE=/' >> "$GITHUB_OUTPUT"
        echo "ARCH=$(cut -d- -f3 <<< "$JOB")" >> "$GITHUB_OUTPUT"
    - name: Auth
      env:
        CRIS_BASE_CI_SSH_PRIVKEY_ED25519: ${{ secrets.CRIS_BASE_CI_SSH_PRIVKEY_ED25519 }}
        CRIS_XCHG_CI_SSH_PRIVKEY_ED25519: ${{ secrets.CRIS_XCHG_CI_SSH_PRIVKEY_ED25519 }}
      run: |
        for repo in 'cris-base' 'cris-xchg'; do
            mkdir -p ~/".ssh/github_deploy_cyfitech/$repo"
            sh -c 'echo "$'"$(echo "$repo" | tr 'a-z' 'A-Z' | sed 's/[^[:alnum:]]/_/g')_CI_SSH_PRIVKEY_ED25519"'"' > ~/".ssh/github_deploy_cyfitech/$repo/id_ed25519"
            chmod -R go-rwx ~/".ssh/github_deploy_cyfitech/$repo"
        done
        scripts/auth_github_ssh.sh
        # Re-route public access to HTTPS and private to SSH.
        git config --global 'url.https://github.com/cyfitech/cris-core.insteadOf' 'git@github.com:cyfitech/cris-core'
        git config --global 'url.git@github.com:cyfitech/cris-base.insteadOf' 'https://github.com/cyfitech/cris-base'
        git config --global 'url.git@github.com:cyfitech/cris-xchg.insteadOf' 'https://github.com/cyfitech/cris-xchg'
    - name: Fetch submodules
      run: |
        GIT_SSH_COMMAND='ssh -vvv' git submodule update --init --jobs 10 --recursive
    - name: Build
      env:
        ARCH: ${{ steps.docker-build-config.outputs.ARCH }}
        BASE_IMAGE: ${{ steps.docker-build-config.outputs.BASE_IMAGE }}
        TAG: ${{ steps.docker-build-config.outputs.TAG }}
      run: |
        set -e
        git submodule foreach --recursive -q pwd                    \
        | cat <(pwd) -                                              \
        | sed -n 's/\(\/cris\-core\)\/*$/\1/p'                      \
        | xargs -I{} find {} -maxdepth 1 -name docker -type d       \
        | sort -u                                                   \
        | head -n1                                                  \
        | xargs -I{} docker build                                   \
            --platform "linux/$ARCH"                                \
            --build-arg "BASE_IMAGE=$BASE_IMAGE"                    \
            --no-cache                                              \
            --pull                                                  \
            --rm                                                    \
            -t "ghcr.io/$TAG:ci-$ARCH"                              \
            {}
    - name: Login to ghcr.io
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      if: ${{ github.event_name != 'pull_request' }}
      run: docker login ghcr.io -u "$GITHUB_ACTOR" -p "$GITHUB_TOKEN"
    - name: Push
      env:
        ARCH: ${{ steps.docker-build-config.outputs.ARCH }}
        TAG: ${{ steps.docker-build-config.outputs.TAG }}
      if: ${{ github.event_name != 'pull_request' }}
      run: docker push "ghcr.io/$TAG:ci-$ARCH"

  docker-debian11-arm64:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
    - uses: actions/checkout@v3
    - name: Config
      id: docker-build-config
      env:
        JOB:  ${{ github.job }}
        REPO: ${{ github.repository }}
      run: |
        set -e
        echo "TAG=$REPO-build-$(cut -d- -f2 <<< "$JOB")" >> "$GITHUB_OUTPUT"
        cut -d- -f2 <<< "$JOB"                      \
        | sed 's/\([0-9]\)/:\1/'                    \
        | sed 's/\([0-9][0-9]\)\([0-9]\)/\1\.\2/'   \
        | sed 's/^/BASE_IMAGE=/' >> "$GITHUB_OUTPUT"
        echo "ARCH=$(cut -d- -f3 <<< "$JOB")" >> "$GITHUB_OUTPUT"
    - name: Auth
      env:
        CRIS_BASE_CI_SSH_PRIVKEY_ED25519: ${{ secrets.CRIS_BASE_CI_SSH_PRIVKEY_ED25519 }}
        CRIS_XCHG_CI_SSH_PRIVKEY_ED25519: ${{ secrets.CRIS_XCHG_CI_SSH_PRIVKEY_ED25519 }}
      run: |
        for repo in 'cris-base' 'cris-xchg'; do
            mkdir -p ~/".ssh/github_deploy_cyfitech/$repo"
            sh -c 'echo "$'"$(echo "$repo" | tr 'a-z' 'A-Z' | sed 's/[^[:alnum:]]/_/g')_CI_SSH_PRIVKEY_ED25519"'"' > ~/".ssh/github_deploy_cyfitech/$repo/id_ed25519"
            chmod -R go-rwx ~/".ssh/github_deploy_cyfitech/$repo"
        done
        scripts/auth_github_ssh.sh
        # Re-route public access to HTTPS and private to SSH.
        git config --global 'url.https://github.com/cyfitech/cris-core.insteadOf' 'git@github.com:cyfitech/cris-core'
        git config --global 'url.git@github.com:cyfitech/cris-base.insteadOf' 'https://github.com/cyfitech/cris-base'
        git config --global 'url.git@github.com:cyfitech/cris-xchg.insteadOf' 'https://github.com/cyfitech/cris-xchg'
    - name: Fetch submodules
      run: |
        GIT_SSH_COMMAND='ssh -vvv' git submodule update --init --jobs 10 --recursive
    - uses: docker/setup-qemu-action@v2
    - name: Build
      env:
        ARCH: ${{ steps.docker-build-config.outputs.ARCH }}
        BASE_IMAGE: ${{ steps.docker-build-config.outputs.BASE_IMAGE }}
        TAG: ${{ steps.docker-build-config.outputs.TAG }}
      run: |
        set -e
        git submodule foreach --recursive -q pwd                    \
        | cat <(pwd) -                                              \
        | sed -n 's/\(\/cris\-core\)\/*$/\1/p'                      \
        | xargs -I{} find {} -maxdepth 1 -name docker -type d       \
        | sort -u                                                   \
        | head -n1                                                  \
        | xargs -I{} docker build                                   \
            --platform "linux/$ARCH"                                \
            --build-arg "BASE_IMAGE=$BASE_IMAGE"                    \
            --no-cache                                              \
            --pull                                                  \
            --rm                                                    \
            -t "ghcr.io/$TAG:ci-$ARCH"                              \
            {}
    - name: Login to ghcr.io
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      if: ${{ github.event_name != 'pull_request' }}
      run: docker login ghcr.io -u "$GITHUB_ACTOR" -p "$GITHUB_TOKEN"
    - name: Push
      env:
        ARCH: ${{ steps.docker-build-config.outputs.ARCH }}
        TAG: ${{ steps.docker-build-config.outputs.TAG }}
      if: ${{ github.event_name != 'pull_request' }}
      run: docker push "ghcr.io/$TAG:ci-$ARCH"

  docker-ubuntu2004-amd64:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
    - uses: actions/checkout@v3
    - name: Config
      id: docker-build-config
      env:
        JOB:  ${{ github.job }}
        REPO: ${{ github.repository }}
      run: |
        set -e
        echo "TAG=$REPO-build-$(cut -d- -f2 <<< "$JOB")" >> "$GITHUB_OUTPUT"
        cut -d- -f2 <<< "$JOB"                      \
        | sed 's/\([0-9]\)/:\1/'                    \
        | sed 's/\([0-9][0-9]\)\([0-9]\)/\1\.\2/'   \
        | sed 's/^/BASE_IMAGE=/' >> "$GITHUB_OUTPUT"
        echo "ARCH=$(cut -d- -f3 <<< "$JOB")" >> "$GITHUB_OUTPUT"
    - name: Auth
      env:
        CRIS_BASE_CI_SSH_PRIVKEY_ED25519: ${{ secrets.CRIS_BASE_CI_SSH_PRIVKEY_ED25519 }}
        CRIS_XCHG_CI_SSH_PRIVKEY_ED25519: ${{ secrets.CRIS_XCHG_CI_SSH_PRIVKEY_ED25519 }}
      run: |
        for repo in 'cris-base' 'cris-xchg'; do
            mkdir -p ~/".ssh/github_deploy_cyfitech/$repo"
            sh -c 'echo "$'"$(echo "$repo" | tr 'a-z' 'A-Z' | sed 's/[^[:alnum:]]/_/g')_CI_SSH_PRIVKEY_ED25519"'"' > ~/".ssh/github_deploy_cyfitech/$repo/id_ed25519"
            chmod -R go-rwx ~/".ssh/github_deploy_cyfitech/$repo"
        done
        scripts/auth_github_ssh.sh
        # Re-route public access to HTTPS and private to SSH.
        git config --global 'url.https://github.com/cyfitech/cris-core.insteadOf' 'git@github.com:cyfitech/cris-core'
        git config --global 'url.git@github.com:cyfitech/cris-base.insteadOf' 'https://github.com/cyfitech/cris-base'
        git config --global 'url.git@github.com:cyfitech/cris-xchg.insteadOf' 'https://github.com/cyfitech/cris-xchg'
    - name: Fetch submodules
      run: |
        GIT_SSH_COMMAND='ssh -vvv' git submodule update --init --jobs 10 --recursive
    - name: Build
      env:
        ARCH: ${{ steps.docker-build-config.outputs.ARCH }}
        BASE_IMAGE: ${{ steps.docker-build-config.outputs.BASE_IMAGE }}
        TAG: ${{ steps.docker-build-config.outputs.TAG }}
      run: |
        set -e
        git submodule foreach --recursive -q pwd                    \
        | cat <(pwd) -                                              \
        | sed -n 's/\(\/cris\-core\)\/*$/\1/p'                      \
        | xargs -I{} find {} -maxdepth 1 -name docker -type d       \
        | sort -u                                                   \
        | head -n1                                                  \
        | xargs -I{} docker build                                   \
            --platform "linux/$ARCH"                                \
            --build-arg "BASE_IMAGE=$BASE_IMAGE"                    \
            --no-cache                                              \
            --pull                                                  \
            --rm                                                    \
            -t "ghcr.io/$TAG:ci-$ARCH"                              \
            {}
    - name: Login to ghcr.io
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      if: ${{ github.event_name != 'pull_request' }}
      run: docker login ghcr.io -u "$GITHUB_ACTOR" -p "$GITHUB_TOKEN"
    - name: Push
      env:
        ARCH: ${{ steps.docker-build-config.outputs.ARCH }}
        TAG: ${{ steps.docker-build-config.outputs.TAG }}
      if: ${{ github.event_name != 'pull_request' }}
      run: docker push "ghcr.io/$TAG:ci-$ARCH"

  docker-ubuntu2004-arm64:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
    - uses: actions/checkout@v3
    - name: Config
      id: docker-build-config
      env:
        JOB:  ${{ github.job }}
        REPO: ${{ github.repository }}
      run: |
        set -e
        echo "TAG=$REPO-build-$(cut -d- -f2 <<< "$JOB")" >> "$GITHUB_OUTPUT"
        cut -d- -f2 <<< "$JOB"                      \
        | sed 's/\([0-9]\)/:\1/'                    \
        | sed 's/\([0-9][0-9]\)\([0-9]\)/\1\.\2/'   \
        | sed 's/^/BASE_IMAGE=/' >> "$GITHUB_OUTPUT"
        echo "ARCH=$(cut -d- -f3 <<< "$JOB")" >> "$GITHUB_OUTPUT"
    - name: Auth
      env:
        CRIS_BASE_CI_SSH_PRIVKEY_ED25519: ${{ secrets.CRIS_BASE_CI_SSH_PRIVKEY_ED25519 }}
        CRIS_XCHG_CI_SSH_PRIVKEY_ED25519: ${{ secrets.CRIS_XCHG_CI_SSH_PRIVKEY_ED25519 }}
      run: |
        for repo in 'cris-base' 'cris-xchg'; do
            mkdir -p ~/".ssh/github_deploy_cyfitech/$repo"
            sh -c 'echo "$'"$(echo "$repo" | tr 'a-z' 'A-Z' | sed 's/[^[:alnum:]]/_/g')_CI_SSH_PRIVKEY_ED25519"'"' > ~/".ssh/github_deploy_cyfitech/$repo/id_ed25519"
            chmod -R go-rwx ~/".ssh/github_deploy_cyfitech/$repo"
        done
        scripts/auth_github_ssh.sh
        # Re-route public access to HTTPS and private to SSH.
        git config --global 'url.https://github.com/cyfitech/cris-core.insteadOf' 'git@github.com:cyfitech/cris-core'
        git config --global 'url.git@github.com:cyfitech/cris-base.insteadOf' 'https://github.com/cyfitech/cris-base'
        git config --global 'url.git@github.com:cyfitech/cris-xchg.insteadOf' 'https://github.com/cyfitech/cris-xchg'
    - name: Fetch submodules
      run: |
        GIT_SSH_COMMAND='ssh -vvv' git submodule update --init --jobs 10 --recursive
    - uses: docker/setup-qemu-action@v2
    - name: Build
      env:
        ARCH: ${{ steps.docker-build-config.outputs.ARCH }}
        BASE_IMAGE: ${{ steps.docker-build-config.outputs.BASE_IMAGE }}
        TAG: ${{ steps.docker-build-config.outputs.TAG }}
      run: |
        set -e
        git submodule foreach --recursive -q pwd                    \
        | cat <(pwd) -                                              \
        | sed -n 's/\(\/cris\-core\)\/*$/\1/p'                      \
        | xargs -I{} find {} -maxdepth 1 -name docker -type d       \
        | sort -u                                                   \
        | head -n1                                                  \
        | xargs -I{} docker build                                   \
            --platform "linux/$ARCH"                                \
            --build-arg "BASE_IMAGE=$BASE_IMAGE"                    \
            --no-cache                                              \
            --pull                                                  \
            --rm                                                    \
            -t "ghcr.io/$TAG:ci-$ARCH"                              \
            {}
    - name: Login to ghcr.io
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      if: ${{ github.event_name != 'pull_request' }}
      run: docker login ghcr.io -u "$GITHUB_ACTOR" -p "$GITHUB_TOKEN"
    - name: Push
      env:
        ARCH: ${{ steps.docker-build-config.outputs.ARCH }}
        TAG: ${{ steps.docker-build-config.outputs.TAG }}
      if: ${{ github.event_name != 'pull_request' }}
      run: docker push "ghcr.io/$TAG:ci-$ARCH"

  docker-ubuntu2204-amd64:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
    - uses: actions/checkout@v3
    - name: Config
      id: docker-build-config
      env:
        JOB:  ${{ github.job }}
        REPO: ${{ github.repository }}
      run: |
        set -e
        echo "TAG=$REPO-build-$(cut -d- -f2 <<< "$JOB")" >> "$GITHUB_OUTPUT"
        cut -d- -f2 <<< "$JOB"                      \
        | sed 's/\([0-9]\)/:\1/'                    \
        | sed 's/\([0-9][0-9]\)\([0-9]\)/\1\.\2/'   \
        | sed 's/^/BASE_IMAGE=/' >> "$GITHUB_OUTPUT"
        echo "ARCH=$(cut -d- -f3 <<< "$JOB")" >> "$GITHUB_OUTPUT"
    - name: Auth
      env:
        CRIS_BASE_CI_SSH_PRIVKEY_ED25519: ${{ secrets.CRIS_BASE_CI_SSH_PRIVKEY_ED25519 }}
        CRIS_XCHG_CI_SSH_PRIVKEY_ED25519: ${{ secrets.CRIS_XCHG_CI_SSH_PRIVKEY_ED25519 }}
      run: |
        for repo in 'cris-base' 'cris-xchg'; do
            mkdir -p ~/".ssh/github_deploy_cyfitech/$repo"
            sh -c 'echo "$'"$(echo "$repo" | tr 'a-z' 'A-Z' | sed 's/[^[:alnum:]]/_/g')_CI_SSH_PRIVKEY_ED25519"'"' > ~/".ssh/github_deploy_cyfitech/$repo/id_ed25519"
            chmod -R go-rwx ~/".ssh/github_deploy_cyfitech/$repo"
        done
        scripts/auth_github_ssh.sh
        # Re-route public access to HTTPS and private to SSH.
        git config --global 'url.https://github.com/cyfitech/cris-core.insteadOf' 'git@github.com:cyfitech/cris-core'
        git config --global 'url.git@github.com:cyfitech/cris-base.insteadOf' 'https://github.com/cyfitech/cris-base'
        git config --global 'url.git@github.com:cyfitech/cris-xchg.insteadOf' 'https://github.com/cyfitech/cris-xchg'
    - name: Fetch submodules
      run: |
        GIT_SSH_COMMAND='ssh -vvv' git submodule update --init --jobs 10 --recursive
    - name: Build
      env:
        ARCH: ${{ steps.docker-build-config.outputs.ARCH }}
        BASE_IMAGE: ${{ steps.docker-build-config.outputs.BASE_IMAGE }}
        TAG: ${{ steps.docker-build-config.outputs.TAG }}
      run: |
        set -e
        git submodule foreach --recursive -q pwd                    \
        | cat <(pwd) -                                              \
        | sed -n 's/\(\/cris\-core\)\/*$/\1/p'                      \
        | xargs -I{} find {} -maxdepth 1 -name docker -type d       \
        | sort -u                                                   \
        | head -n1                                                  \
        | xargs -I{} docker build                                   \
            --platform "linux/$ARCH"                                \
            --build-arg "BASE_IMAGE=$BASE_IMAGE"                    \
            --no-cache                                              \
            --pull                                                  \
            --rm                                                    \
            -t "ghcr.io/$TAG:ci-$ARCH"                              \
            {}
    - name: Login to ghcr.io
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      if: ${{ github.event_name != 'pull_request' }}
      run: docker login ghcr.io -u "$GITHUB_ACTOR" -p "$GITHUB_TOKEN"
    - name: Push
      env:
        ARCH: ${{ steps.docker-build-config.outputs.ARCH }}
        TAG: ${{ steps.docker-build-config.outputs.TAG }}
      if: ${{ github.event_name != 'pull_request' }}
      run: docker push "ghcr.io/$TAG:ci-$ARCH"

  docker-ubuntu2204-arm64:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
    - uses: actions/checkout@v3
    - name: Config
      id: docker-build-config
      env:
        JOB:  ${{ github.job }}
        REPO: ${{ github.repository }}
      run: |
        set -e
        echo "TAG=$REPO-build-$(cut -d- -f2 <<< "$JOB")" >> "$GITHUB_OUTPUT"
        cut -d- -f2 <<< "$JOB"                      \
        | sed 's/\([0-9]\)/:\1/'                    \
        | sed 's/\([0-9][0-9]\)\([0-9]\)/\1\.\2/'   \
        | sed 's/^/BASE_IMAGE=/' >> "$GITHUB_OUTPUT"
        echo "ARCH=$(cut -d- -f3 <<< "$JOB")" >> "$GITHUB_OUTPUT"
    - name: Auth
      env:
        CRIS_BASE_CI_SSH_PRIVKEY_ED25519: ${{ secrets.CRIS_BASE_CI_SSH_PRIVKEY_ED25519 }}
        CRIS_XCHG_CI_SSH_PRIVKEY_ED25519: ${{ secrets.CRIS_XCHG_CI_SSH_PRIVKEY_ED25519 }}
      run: |
        for repo in 'cris-base' 'cris-xchg'; do
            mkdir -p ~/".ssh/github_deploy_cyfitech/$repo"
            sh -c 'echo "$'"$(echo "$repo" | tr 'a-z' 'A-Z' | sed 's/[^[:alnum:]]/_/g')_CI_SSH_PRIVKEY_ED25519"'"' > ~/".ssh/github_deploy_cyfitech/$repo/id_ed25519"
            chmod -R go-rwx ~/".ssh/github_deploy_cyfitech/$repo"
        done
        scripts/auth_github_ssh.sh
        # Re-route public access to HTTPS and private to SSH.
        git config --global 'url.https://github.com/cyfitech/cris-core.insteadOf' 'git@github.com:cyfitech/cris-core'
        git config --global 'url.git@github.com:cyfitech/cris-base.insteadOf' 'https://github.com/cyfitech/cris-base'
        git config --global 'url.git@github.com:cyfitech/cris-xchg.insteadOf' 'https://github.com/cyfitech/cris-xchg'
    - name: Fetch submodules
      run: |
        GIT_SSH_COMMAND='ssh -vvv' git submodule update --init --jobs 10 --recursive
    - uses: docker/setup-qemu-action@v2
    - name: Build
      env:
        ARCH: ${{ steps.docker-build-config.outputs.ARCH }}
        BASE_IMAGE: ${{ steps.docker-build-config.outputs.BASE_IMAGE }}
        TAG: ${{ steps.docker-build-config.outputs.TAG }}
      run: |
        set -e
        git submodule foreach --recursive -q pwd                    \
        | cat <(pwd) -                                              \
        | sed -n 's/\(\/cris\-core\)\/*$/\1/p'                      \
        | xargs -I{} find {} -maxdepth 1 -name docker -type d       \
        | sort -u                                                   \
        | head -n1                                                  \
        | xargs -I{} docker build                                   \
            --platform "linux/$ARCH"                                \
            --build-arg "BASE_IMAGE=$BASE_IMAGE"                    \
            --no-cache                                              \
            --pull                                                  \
            --rm                                                    \
            -t "ghcr.io/$TAG:ci-$ARCH"                              \
            {}
    - name: Login to ghcr.io
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      if: ${{ github.event_name != 'pull_request' }}
      run: docker login ghcr.io -u "$GITHUB_ACTOR" -p "$GITHUB_TOKEN"
    - name: Push
      env:
        ARCH: ${{ steps.docker-build-config.outputs.ARCH }}
        TAG: ${{ steps.docker-build-config.outputs.TAG }}
      if: ${{ github.event_name != 'pull_request' }}
      run: docker push "ghcr.io/$TAG:ci-$ARCH"

